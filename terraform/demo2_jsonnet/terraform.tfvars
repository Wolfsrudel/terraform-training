aws_region = "eu-west-1"

# NOTE:
# You should specify list of users in users.tf.jsonnet, because jsonnet is used in this example to generate configurations
#
# Working with lists in Terraform can be not-trivial task if you don't want to recreate (and/or loose) already created data
